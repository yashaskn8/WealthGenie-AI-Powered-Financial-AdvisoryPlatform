apiVersion: batch/v1
kind: Job
metadata:
  generateName: wealthgenie-phase2-index-migration-
  namespace: wealthgenie
  labels:
    app: wealthgenie-phase2-index-migration
    app.kubernetes.io/name: wealthgenie-phase2-index-migration
spec:
  completions: 1
  parallelism: 1
  backoffLimit: 0
  activeDeadlineSeconds: 240
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app: wealthgenie-phase2-index-migration
    spec:
      automountServiceAccountToken: false
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: phase2-index-migration
          image: wealthgenie-server:latest
          imagePullPolicy: IfNotPresent
          command: ["node", "scripts/migratePhase2Indexes.js"]
          env:
            - name: NODE_ENV
              value: production
            - name: MONGODB_MIGRATION_URI
              valueFrom:
                secretKeyRef:
                  name: wealthgenie-secrets
                  key: MONGODB_URI
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
