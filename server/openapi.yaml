openapi: 3.1.0
info:
  title: WealthGenie Public API
  version: 2.0.0
  description: Canonical contract for the supported public Express API. Internal metrics and MCP routes are intentionally excluded.
servers:
  - { url: 'http://localhost:5000', description: Local development }
  - { url: '/', description: Same-origin production gateway }

paths:
  /api/auth/register:
    post:
      operationId: registerUser
      tags: [Authentication]
      security: []
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/Register' }
      responses: { '201': { $ref: '#/components/responses/AuthSession' }, '400': { $ref: '#/components/responses/BadRequest' }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/auth/login:
    post:
      operationId: loginUser
      tags: [Authentication]
      security: []
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/Login' }
      responses: { '200': { $ref: '#/components/responses/AuthSession' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }
  /api/auth/logout:
    post:
      operationId: logoutUser
      tags: [Authentication]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      responses: { '200': { $ref: '#/components/responses/Message' }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/auth/session:
    get:
      operationId: restoreSession
      tags: [Authentication]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      responses: { '200': { $ref: '#/components/responses/AuthSession' }, '401': { $ref: '#/components/responses/Unauthorized' } }

  /api/profile/build:
    post:
      operationId: buildProfile
      tags: [Profiles]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      x-idempotency-key: required
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { $ref: '#/components/requestBodies/ProfileCreate' }
      responses: { '201': { $ref: '#/components/responses/Profile' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '409': { $ref: '#/components/responses/Conflict' }, '429': { $ref: '#/components/responses/Error' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/profile/precompute:
    post:
      operationId: precomputeProfileRecommendation
      tags: [Profiles]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      x-idempotency-key: optional
      description: Server-side, short-lived non-authoritative recommendation precomputation. It does not persist profile, recommendation, or audit records.
      requestBody: { $ref: '#/components/requestBodies/ProfileCreate' }
      responses: { '200': { $ref: '#/components/responses/ProfileCandidate' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/profile/complete:
    post:
      operationId: completeFinancialProfile
      tags: [Profiles]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      x-idempotency-key: required
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      description: Authoritative profile completion boundary. Commits the profile, recommendation, audit record, and idempotency result atomically. The financial response is reconstructed from canonical current state; if a newer generation wins before response construction, operation_result identifies the committed generation separately. Advisory prose remains deferred.
      requestBody: { $ref: '#/components/requestBodies/ProfileCompletion' }
      responses: { '200': { $ref: '#/components/responses/ProfileCompletion' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '409': { $ref: '#/components/responses/Conflict' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/profile/current:
    get:
      operationId: getCurrentProfile
      tags: [Profiles]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      responses: { '200': { $ref: '#/components/responses/Profile' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/profile/{profileId}/health-score:
    get:
      operationId: getFinancialHealthScore
      tags: [Profiles]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/ProfileId' }]
      responses: { '200': { $ref: '#/components/responses/Object' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/profile/{profileId}:
    put:
      operationId: updateProfile
      tags: [Profiles]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      x-idempotency-key: required
      parameters: [{ $ref: '#/components/parameters/ProfileId' }, { $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { $ref: '#/components/requestBodies/ProfileUpdate' }
      description: Atomically commits the profile revision and its replacement canonical recommendation, allocation revision, pointer, audit entry, and idempotency result. Recommendation computation runs before the short commit transaction.
      responses: { '200': { $ref: '#/components/responses/ProfileUpdate' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' }, '422': { $ref: '#/components/responses/Unprocessable' }, '502': { $ref: '#/components/responses/BadGateway' }, '503': { $ref: '#/components/responses/Unavailable' } }

  /api/recommend:
    post:
      operationId: createRecommendation
      tags: [Advisory]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      x-idempotency-key: required
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      description: Commits a recommendation and audit record atomically. The response represents canonical current state; if a newer generation wins before response construction, operation_result identifies the generation committed by this operation.
      requestBody: { $ref: '#/components/requestBodies/Recommendation' }
      responses: { '200': { $ref: '#/components/responses/Recommendation' }, '201': { $ref: '#/components/responses/Recommendation' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '409': { $ref: '#/components/responses/Conflict' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/recommend/current:
    get:
      operationId: getCurrentRecommendation
      tags: [Advisory]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ name: profileId, in: query, required: true, schema: { type: string, pattern: '^[0-9a-fA-F]{24}$' } }]
      responses: { '200': { $ref: '#/components/responses/Recommendation' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/recommend/audit:
    get:
      operationId: listRecommendationAudits
      tags: [Advisory]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters:
        - { name: profileId, in: query, schema: { type: string, pattern: '^[0-9a-fA-F]{24}$' } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100, x-raw-query-pattern: '^(0|[1-9][0-9]*)$' } }
        - { name: skip, in: query, schema: { type: integer, minimum: 0, maximum: 10000000, x-raw-query-pattern: '^(0|[1-9][0-9]*)$' } }
        - { name: correlationId, in: query, schema: { type: string, pattern: '^[A-Za-z0-9._:-]{1,128}$' } }
      responses: { '200': { description: Owned paginated audit records, content: { application/json: { schema: { $ref: '#/components/schemas/AuditListResponse' } } } }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/recommend/audit/verify:
    get:
      operationId: verifyRecommendationAuditChain
      tags: [Advisory]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      responses:
        '200': { description: Valid tamper-evident chain, content: { application/json: { schema: { $ref: '#/components/schemas/AuditVerification' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '409': { description: Chain verification found an integrity discrepancy, content: { application/json: { schema: { $ref: '#/components/schemas/AuditVerification' } } } }
        '503': { $ref: '#/components/responses/Unavailable' }
  /api/recommend/audit/{id}:
    get:
      operationId: getRecommendationAudit
      tags: [Advisory]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: Owned audit record, content: { application/json: { schema: { $ref: '#/components/schemas/AuditMetadata' } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/recommend/weights:
    post:
      operationId: updateRecommendationWeights
      tags: [Advisory]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/RecommendationWeights' }
      responses: { '200': { $ref: '#/components/responses/Recommendation' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/recommend/{recommendationId}/advisory:
    post:
      operationId: generateDeferredAdvisory
      tags: [Advisory]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ name: recommendationId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F]{24}$' } }]
      responses: { '200': { $ref: '#/components/responses/DeferredAdvisory' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' } }

  /api/agent/plan-review:
    post:
      operationId: enqueuePlanReview
      tags: [Agent]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      description: Bounded, read-only evidence review. It never changes profiles, recommendations, goals, allocations, or weights. The feature is controlled by AGENTIC_PLAN_REVIEW_ENABLED.
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PlanReviewRequest' } } } }
      responses: { '202': { $ref: '#/components/responses/PlanReviewRun' }, '200': { $ref: '#/components/responses/PlanReviewRun' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '429': { $ref: '#/components/responses/Error' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/agent/plan-review/current:
    get:
      operationId: getCurrentPlanReviewRun
      tags: [Agent]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ name: profileId, in: query, required: true, schema: { type: string, pattern: '^[0-9a-fA-F]{24}$' } }]
      responses: { '200': { $ref: '#/components/responses/PlanReviewRun' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/agent/cards:
    get:
      operationId: listAgentCards
      tags: [Agent]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      description: Allowlisted A2A-inspired internal capability cards for the Plan Review and Evidence Verifier agents; official A2A conformance is not claimed.
      responses: { '200': { $ref: '#/components/responses/Object' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/agent/cards/{agentType}:
    get:
      operationId: getAgentCard
      tags: [Agent]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ name: agentType, in: path, required: true, schema: { type: string, enum: [PLAN_REVIEW, EVIDENCE_VERIFIER] } }]
      responses: { '200': { $ref: '#/components/responses/Object' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/agent/plan-review/{runId}:
    get:
      operationId: getPlanReviewRun
      tags: [Agent]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ name: runId, in: path, required: true, schema: { type: string, format: uuid } }]
      responses: { '200': { $ref: '#/components/responses/PlanReviewRun' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/agent/plan-review/{runId}/events:
    get:
      operationId: streamPlanReviewEvents
      tags: [Agent]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters:
        - { name: runId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: Last-Event-ID, in: header, required: false, schema: { type: integer, minimum: 0 } }
      responses: { '200': { description: Owned AG-UI-inspired internal SSE event stream (not the official AG-UI protocol), content: { text/event-stream: { schema: { type: string } } } }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/agent/plan-review/{runId}/ui:
    get:
      operationId: getPlanReviewA2UI
      tags: [Agent]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ name: runId, in: path, required: true, schema: { type: string, format: uuid } }]
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/agent/plan-review/{runId}/cancel:
    post:
      operationId: cancelPlanReviewRun
      tags: [Agent]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ name: runId, in: path, required: true, schema: { type: string, format: uuid } }]
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/agent/plan-review/{runId}/action:
    post:
      operationId: actOnPlanReview
      tags: [Agent]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ name: runId, in: path, required: true, schema: { type: string, format: uuid } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [action], properties: { action: { type: string, enum: [APPROVE_RECOMPUTE, REJECT_RECOMPUTE, OPEN_PROFILE, OPEN_GOALS] } } } } } }
      responses: { '200': { $ref: '#/components/responses/Object' }, '201': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/agent/mandates:
    get:
      operationId: listUserIntentMandates
      tags: [AgentAuthorization]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      description: Read-only list of owned AP2-inspired verifiable action mandates. This is not a payment mandate API.
      parameters: [{ name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 100, x-raw-query-pattern: '^(0|[1-9][0-9]*)$' } }]
      responses: { '200': { description: Owned mandates, content: { application/json: { schema: { type: object, required: [mandates], properties: { mandates: { type: array, items: { $ref: '#/components/schemas/UserIntentMandate' } } } } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/agent/passkeys/registration/options:
    post:
      operationId: createPasskeyRegistrationOptions
      tags: [AgentAuthorization]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      description: Creates WebAuthn passkey registration options for the authenticated user. Private keys and biometric data never reach WealthGenie.
      responses: { '200': { $ref: '#/components/responses/Object' }, '401': { $ref: '#/components/responses/Unauthorized' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/agent/passkeys/registration/verify:
    post:
      operationId: verifyPasskeyRegistration
      tags: [AgentAuthorization]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PasskeyRegistrationResponse' } } } }
      responses: { '201': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '409': { $ref: '#/components/responses/Conflict' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/agent/mandates/{mandateId}:
    get:
      operationId: getUserIntentMandate
      tags: [AgentAuthorization]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ name: mandateId, in: path, required: true, schema: { type: string, format: uuid } }]
      responses: { '200': { $ref: '#/components/responses/UserIntentMandate' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/agent/mandates/{mandateId}/approval/options:
    post:
      operationId: createMandateApprovalOptions
      tags: [AgentAuthorization]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ name: mandateId, in: path, required: true, schema: { type: string, format: uuid } }]
      description: Creates one-time step-up approval options bound to one mandate hash. Production uses WebAuthn/passkeys and fails closed when unavailable.
      responses: { '200': { $ref: '#/components/responses/Object' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' }, '428': { $ref: '#/components/responses/Error' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/agent/mandates/{mandateId}/approval/verify:
    post:
      operationId: verifyMandateApproval
      tags: [AgentAuthorization]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ name: mandateId, in: path, required: true, schema: { type: string, format: uuid } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/MandateApprovalAssertion' } } } }
      responses: { '200': { $ref: '#/components/responses/UserIntentMandate' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '409': { $ref: '#/components/responses/Conflict' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/agent/mandates/{mandateId}/revoke:
    post:
      operationId: revokeUserIntentMandate
      tags: [AgentAuthorization]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ name: mandateId, in: path, required: true, schema: { type: string, format: uuid } }]
      requestBody: { required: false, content: { application/json: { schema: { type: object, additionalProperties: false, properties: { reason: { type: string, maxLength: 240 } } } } } }
      responses: { '200': { $ref: '#/components/responses/UserIntentMandate' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/agent/mandates/{mandateId}/execute:
    post:
      operationId: executeAuthorizedAction
      tags: [AgentAuthorization]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ name: mandateId, in: path, required: true, schema: { type: string, format: uuid } }]
      description: Executes only a cryptographically authorized, single-use APPROVE_RECOMPUTE mandate. The request body must be empty; no model-controlled parameters are accepted.
      responses: { '200': { $ref: '#/components/responses/ExecutionResult' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/agent/receipts/{receiptId}:
    get:
      operationId: getExecutionReceipt
      tags: [AgentAuthorization]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ name: receiptId, in: path, required: true, schema: { type: string, format: uuid } }]
      responses: { '200': { $ref: '#/components/responses/ExecutionReceipt' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/agent/receipts/{receiptId}/verify:
    get:
      operationId: verifyExecutionReceipt
      tags: [AgentAuthorization]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ name: receiptId, in: path, required: true, schema: { type: string, format: uuid } }]
      responses: { '200': { description: Cryptographic receipt verification result, content: { application/json: { schema: { type: object } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/agent/plan-health:
    get:
      operationId: inspectPlanHealth
      tags: [Agent]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ name: profileId, in: query, required: true, schema: { type: string, pattern: '^[0-9a-fA-F]{24}$' } }]
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }
  /api/agent/plan-health/events:
    get:
      operationId: listPlanHealthEvents
      tags: [Agent]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 50, x-raw-query-pattern: '^(0|[1-9][0-9]*)$' } }]
      responses: { '200': { $ref: '#/components/responses/Object' }, '401': { $ref: '#/components/responses/Unauthorized' } }
  /api/agent/plan-health/events/{eventId}/acknowledge:
    patch:
      operationId: acknowledgePlanHealthEvent
      tags: [Agent]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ name: eventId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F]{24}$' } }]
      responses: { '200': { $ref: '#/components/responses/Object' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }

  /api/goals/create:
    post:
      operationId: createGoal
      tags: [Goals]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      x-idempotency-key: required
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { $ref: '#/components/requestBodies/GoalCreate' }
      responses: { '201': { $ref: '#/components/responses/GoalCreate' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '409': { $ref: '#/components/responses/Conflict' }, '500': { $ref: '#/components/responses/Error' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/goals:
    get:
      operationId: listGoals
      tags: [Goals]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      responses:
        '200':
          description: Owned goals with calculation and advisory freshness bound to their source financial state.
          content:
            application/json:
              schema:
                type: object
                required: [goals]
                properties:
                  goals: { type: array, items: { $ref: '#/components/schemas/Goal' } }
        '401': { $ref: '#/components/responses/Unauthorized' }
  /api/goals/{goalId}/refresh-advice:
    patch:
      operationId: refreshGoalAdvice
      tags: [Goals]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ $ref: '#/components/parameters/GoalId' }]
      responses: { '200': { $ref: '#/components/responses/GoalAdvice' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/goals/{goalId}/simulate:
    post:
      operationId: simulateGoalContribution
      tags: [Goals]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ $ref: '#/components/parameters/GoalId' }]
      requestBody: { $ref: '#/components/requestBodies/GoalSimulation' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/goals/{goalId}:
    patch:
      operationId: updateGoal
      tags: [Goals]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ $ref: '#/components/parameters/GoalId' }]
      requestBody: { $ref: '#/components/requestBodies/GoalUpdate' }
      responses: { '200': { $ref: '#/components/responses/GoalUpdate' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' }, '503': { $ref: '#/components/responses/Unavailable' } }
    delete:
      operationId: deleteGoal
      tags: [Goals]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ $ref: '#/components/parameters/GoalId' }, { $ref: '#/components/parameters/GoalIfMatch' }]
      responses: { '200': { $ref: '#/components/responses/GoalDelete' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' }, '428': { $ref: '#/components/responses/PreconditionRequired' }, '503': { $ref: '#/components/responses/Unavailable' } }

  /api/instruments:
    get:
      operationId: listInstruments
      tags: [Instruments]
      security: []
      parameters:
        - { name: type, in: query, schema: { type: string, enum: [FD, Mutual_Fund, ETF, Government, ELSS] } }
        - { name: sort, in: query, schema: { type: string, enum: [name, interestRate, rate, returns1yr, returns3yr, returns5yr, riskLevel, aumCr, expenseRatio] } }
        - { name: order, in: query, schema: { type: string, enum: [asc, desc] } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100, x-raw-query-pattern: '^(0|[1-9][0-9]*)$' } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, maximum: 100000, x-raw-query-pattern: '^(0|[1-9][0-9]*)$' } }
      responses: { '200': { description: Canonical paginated public investment catalog, content: { application/json: { schema: { $ref: '#/components/schemas/InstrumentPage' } } } }, '400': { $ref: '#/components/responses/BadRequest' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/instruments/rank-wti:
    post:
      operationId: rankInstruments
      description: Returns zero to five products from a source-qualified universe after parent-category hard suitability. Phase 2 ranks supported mutual-fund Growth options only when verified AMFI current and approximately one-year historical NAV observations establish an order; otherwise it returns explicitly comparable or unavailable results.
      tags: [Instruments]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/RankInstruments' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }

  /api/projection:
    post:
      operationId: createProjection
      tags: [Projections]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/Projection' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }
  /api/projection/compare:
    post:
      operationId: compareInvestmentProjection
      tags: [Projections]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/ProjectionComparison' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }
  /api/projection/allocation-split:
    post:
      operationId: calculateAllocationSplit
      tags: [Projections]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/AllocationSplit' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }
  /api/projection/custom-portfolio:
    post:
      operationId: createCustomPortfolioProjection
      tags: [Projections]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/CustomPortfolioProjection' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }
  /api/projection/stress-test:
    post:
      operationId: runInstrumentStressTest
      tags: [Projections]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/StressScenario' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/projection/step-up:
    post:
      operationId: calculateStepUpProjection
      tags: [Projections]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/StepUpProjection' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }
  /api/projection/xirr:
    post:
      operationId: calculateXirr
      tags: [Projections]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/Xirr' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }
  /api/montecarlo/montecarlo:
    post:
      operationId: runMonteCarlo
      tags: [Projections]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/MonteCarlo' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }
  /api/montecarlo/portfolio:
    post:
      operationId: runPortfolioMonteCarlo
      tags: [Projections]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/PortfolioMonteCarlo' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }

  /api/market/rates:
    get: { operationId: getMarketRates, tags: [Market], security: [], responses: { '200': { $ref: '#/components/responses/Object' } } }
  /api/market/benchmarks:
    get: { operationId: getMarketBenchmarks, tags: [Market], security: [], responses: { '200': { $ref: '#/components/responses/Object' } } }
  /api/market/government-schemes:
    get: { operationId: getGovernmentSchemeFacts, tags: [Market], security: [], responses: { '200': { $ref: '#/components/responses/Object' } } }
  /api/market/fixed-deposits/sbi:
    get: { operationId: getSbiFixedDepositFacts, tags: [Market], security: [], responses: { '200': { $ref: '#/components/responses/Object' } } }
  /api/market/mutual-funds/nav:
    get:
      operationId: getMutualFundNavFacts
      tags: [Market]
      security: []
      parameters:
        - name: schemeCodes
          in: query
          required: true
          schema: { type: string, pattern: '^\d+(,\d+){0,49}$', maxLength: 600 }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' } }
  /api/market/params:
    get: { operationId: getMarketParameters, tags: [Market], security: [], responses: { '200': { $ref: '#/components/responses/Object' } } }
  /api/market/refresh:
    post:
      operationId: refreshMarketData
      tags: [Market]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      responses: { '202': { $ref: '#/components/responses/Object' }, '401': { $ref: '#/components/responses/Unauthorized' } }

  /api/tax/compute:
    get:
      operationId: computeTax
      tags: [Tax]
      security: []
      parameters:
        - { $ref: '#/components/parameters/Income' }
        - { $ref: '#/components/parameters/IncomeSource' }
        - { $ref: '#/components/parameters/FiscalYear' }
        - { name: age, in: query, required: false, schema: { type: integer, minimum: 18, maximum: 120 } }
        - { name: regime, in: query, required: true, schema: { type: string, enum: [new, old] } }
        - { name: section80C, in: query, required: false, schema: { type: number, minimum: 0, maximum: 150000 } }
        - { name: nps80CCD1B, in: query, required: false, schema: { type: number, minimum: 0, maximum: 50000 } }
        - { name: section80CCD, in: query, required: false, schema: { type: number, minimum: 0, maximum: 50000 } }
        - { name: nps80CCD2, in: query, required: false, schema: { type: number, minimum: 0, maximum: 100000000 } }
        - { name: basicSalary, in: query, required: false, schema: { type: number, minimum: 0, maximum: 1000000000 } }
        - { name: isGovtEmployee, in: query, required: false, schema: { type: boolean } }
        - { name: section80D, in: query, required: false, schema: { type: number, minimum: 0, maximum: 100000 } }
        - { name: section80D_self, in: query, required: false, schema: { type: number, minimum: 0, maximum: 50000 } }
        - { name: section80D_parents, in: query, required: false, schema: { type: number, minimum: 0, maximum: 50000 } }
        - { name: parents_senior, in: query, required: false, schema: { type: boolean } }
        - { name: self_senior, in: query, required: false, schema: { type: boolean } }
        - { name: hra, in: query, required: false, schema: { type: number, minimum: 0, maximum: 100000000 } }
        - { name: homeLoanInterest, in: query, required: false, schema: { type: number, minimum: 0, maximum: 200000 } }
        - { name: section80EEA, in: query, required: false, schema: { type: number, minimum: 0, maximum: 150000 } }
        - { name: savingsInterest, in: query, required: false, schema: { type: number, minimum: 0, maximum: 100000000 } }
        - { name: section80TTA, in: query, required: false, schema: { type: number, minimum: 0, maximum: 10000 } }
        - { name: section80TTB, in: query, required: false, schema: { type: number, minimum: 0, maximum: 50000 } }
        - { name: other, in: query, required: false, schema: { type: number, const: 0 } }
      responses: { '200': { $ref: '#/components/responses/TaxCalculation' }, '400': { $ref: '#/components/responses/BadRequest' }, '422': { $ref: '#/components/responses/Unprocessable' } }
  /api/tax/policies:
    get:
      operationId: getTaxPolicyMetadata
      tags: [Tax]
      security: []
      responses: { '200': { $ref: '#/components/responses/Object' } }
  /api/tax/compare:
    get:
      operationId: compareTaxRegimes
      tags: [Tax]
      security: []
      parameters:
        - { $ref: '#/components/parameters/Income' }
        - { $ref: '#/components/parameters/IncomeSource' }
        - { $ref: '#/components/parameters/FiscalYear' }
        - { name: age, in: query, required: true, schema: { type: integer, minimum: 18, maximum: 120 } }
        - { name: section80C, in: query, required: false, schema: { type: number, minimum: 0, maximum: 150000 } }
        - { name: nps80CCD1B, in: query, required: false, schema: { type: number, minimum: 0, maximum: 50000 } }
        - { name: section80CCD, in: query, required: false, schema: { type: number, minimum: 0, maximum: 50000 } }
        - { name: nps80CCD2, in: query, required: false, schema: { type: number, minimum: 0, maximum: 100000000 } }
        - { name: basicSalary, in: query, required: false, schema: { type: number, minimum: 0, maximum: 1000000000 } }
        - { name: isGovtEmployee, in: query, required: false, schema: { type: boolean } }
        - { name: section80D, in: query, required: false, schema: { type: number, minimum: 0, maximum: 100000 } }
        - { name: section80D_self, in: query, required: false, schema: { type: number, minimum: 0, maximum: 50000 } }
        - { name: section80D_parents, in: query, required: false, schema: { type: number, minimum: 0, maximum: 50000 } }
        - { name: parents_senior, in: query, required: false, schema: { type: boolean } }
        - { name: self_senior, in: query, required: false, schema: { type: boolean } }
        - { name: hra, in: query, required: false, schema: { type: number, minimum: 0, maximum: 100000000 } }
        - { name: homeLoanInterest, in: query, required: false, schema: { type: number, minimum: 0, maximum: 200000 } }
        - { name: section80EEA, in: query, required: false, schema: { type: number, minimum: 0, maximum: 150000 } }
        - { name: savingsInterest, in: query, required: false, schema: { type: number, minimum: 0, maximum: 100000000 } }
        - { name: section80TTA, in: query, required: false, schema: { type: number, minimum: 0, maximum: 10000 } }
        - { name: section80TTB, in: query, required: false, schema: { type: number, minimum: 0, maximum: 50000 } }
        - { name: other, in: query, required: false, schema: { type: number, const: 0 } }
      responses: { '200': { $ref: '#/components/responses/TaxComparison' }, '400': { $ref: '#/components/responses/BadRequest' }, '422': { $ref: '#/components/responses/Unprocessable' } }
  /api/tax/post-tax-return:
    post:
      operationId: calculatePostTaxReturn
      tags: [Tax]
      security: []
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/PostTaxReturn' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' } }
  /api/tax/post-tax-return/batch:
    post:
      operationId: calculatePostTaxReturns
      tags: [Tax]
      security: []
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/PostTaxReturnBatch' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' } }

  /api/portfolio/optimise:
    post:
      operationId: optimisePortfolio
      tags: [Portfolio]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/PortfolioOptimise' }
      responses: { '200': { $ref: '#/components/responses/Portfolio' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }
  /api/portfolio/rebalance:
    post:
      operationId: rebalancePortfolio
      tags: [Portfolio]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/Rebalance' }
      responses: { '200': { $ref: '#/components/responses/Portfolio' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' } }

  /api/chat/message:
    post:
      operationId: sendChatMessage
      tags: [Chat]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/Chat' }
      responses: { '200': { $ref: '#/components/responses/Chat' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '409': { $ref: '#/components/responses/Conflict' }, '429': { $ref: '#/components/responses/Error' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/chat/history:
    get:
      operationId: getChatHistory
      tags: [Chat]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      parameters: [{ name: session_id, in: query, schema: { type: string, pattern: '^[A-Za-z0-9._:-]{1,100}$' } }, { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 200, x-raw-query-pattern: '^(0|[1-9][0-9]*)$' } }]
      responses: { '200': { description: Current user's history envelope, content: { application/json: { schema: { $ref: '#/components/schemas/ChatHistoryResponse' } } } }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '503': { $ref: '#/components/responses/Unavailable' } }
  /api/chat/session/{sessionId}:
    delete:
      operationId: deleteChatSession
      tags: [Chat]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      parameters: [{ name: sessionId, in: path, required: true, schema: { type: string, pattern: '^[A-Za-z0-9._:-]{1,100}$' } }]
      responses: { '200': { $ref: '#/components/responses/Message' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '503': { $ref: '#/components/responses/Unavailable' } }

  /api/regime/current:
    get: { operationId: getCurrentMarketContext, tags: [Regime], security: [], responses: { '200': { description: Verified market context and additive market snapshot, content: { application/json: { schema: { $ref: '#/components/schemas/MarketContextResponse' } } } } } }
  /api/regime/adjust:
    post:
      operationId: previewProfileSafeMarketContextAdjustment
      tags: [Regime]
      security: [{ CookieAuth: [] }, { BearerAuth: [] }]
      x-csrf-protection: cookie-session
      requestBody: { $ref: '#/components/requestBodies/RegimeAdjust' }
      responses: { '200': { $ref: '#/components/responses/Object' }, '400': { $ref: '#/components/responses/BadRequest' }, '401': { $ref: '#/components/responses/Unauthorized' }, '404': { $ref: '#/components/responses/NotFound' } }

  /health:
    get: { operationId: getHealth, tags: [Health], security: [], responses: { '200': { $ref: '#/components/responses/Health' } } }
  /health/deep:
    get: { operationId: getDeepHealth, tags: [Health], security: [], responses: { '200': { $ref: '#/components/responses/Health' }, '503': { $ref: '#/components/responses/Unavailable' } } }
  /health/ready:
    get: { operationId: getReadiness, tags: [Health], security: [], responses: { '200': { $ref: '#/components/responses/Health' }, '503': { $ref: '#/components/responses/Unavailable' } } }
  /health/live:
    get: { operationId: getLiveness, tags: [Health], security: [], responses: { '200': { $ref: '#/components/responses/Health' } } }
  /ready:
    get: { operationId: redirectReadiness, tags: [Health], security: [], responses: { '307': { description: Redirect to /health/ready } } }
  /live:
    get: { operationId: redirectLiveness, tags: [Health], security: [], responses: { '307': { description: Redirect to /health/live } } }
  /healthz:
    get: { operationId: getLegacyLiveness, tags: [Health], security: [], responses: { '200': { $ref: '#/components/responses/Health' } } }
  /readyz:
    get: { operationId: redirectLegacyReadiness, tags: [Health], security: [], responses: { '307': { description: Redirect to /health/ready } } }
  /api/health:
    get: { operationId: getApiHealth, tags: [Health], security: [], responses: { '200': { $ref: '#/components/responses/Health' } } }

components:
  securitySchemes:
    CookieAuth: { type: apiKey, in: cookie, name: wg_session, description: Secure HttpOnly browser session cookie. }
    BearerAuth: { type: http, scheme: bearer, bearerFormat: JWT, description: Explicitly configured non-browser compatibility authentication. }
  parameters:
    IdempotencyKey: { name: Idempotency-Key, in: header, required: true, schema: { type: string, pattern: '^[A-Za-z0-9._:-]{8,128}$', minLength: 8, maxLength: 128 } }
    ProfileId: { name: profileId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F]{24}$' } }
    GoalId: { name: goalId, in: path, required: true, schema: { type: string, pattern: '^[0-9a-fA-F]{24}$' } }
    GoalIfMatch: { name: If-Match, in: header, required: true, description: Expected current goal version. The server atomically compares this version before deletion., schema: { type: string, pattern: '^(W/)?\"?[1-9][0-9]*\"?$' } }
    Income: { name: income, in: query, required: true, schema: { type: number, minimum: 0, maximum: 1000000000 } }
    IncomeSource: { name: incomeSource, in: query, required: true, schema: { type: string, enum: [salary, pension, family_pension, business, other] } }
    FiscalYear: { name: fiscalYear, in: query, required: true, schema: { type: string, enum: [FY2025-26, FY2026-27] } }
  requestBodies:
    Register: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/RegisterRequest' } } } }
    Login: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/LoginRequest' } } } }
    ProfileCreate: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/FinancialProfileInput' } } } }
    ProfileCompletion: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/FinancialProfileCompletion' } } } }
    ProfileUpdate: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/FinancialProfileUpdate' } } } }
    Recommendation: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/RecommendationRequest' } } } }
    RecommendationWeights: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/RecommendationWeightsRequest' } } } }
    GoalCreate: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/GoalCreateRequest' } } } }
    GoalUpdate: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/GoalUpdateRequest' } } } }
    GoalSimulation: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/GoalSimulationRequest' } } } }
    RankInstruments: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/RankInstrumentsRequest' } } } }
    Projection: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ProjectionRequest' } } } }
    ProjectionComparison: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ProjectionComparisonRequest' } } } }
    AllocationSplit: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AllocationSplitRequest' } } } }
    CustomPortfolioProjection: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CustomPortfolioProjectionRequest' } } } }
    StressScenario: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/StressScenarioRequest' } } } }
    StepUpProjection: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/StepUpProjectionRequest' } } } }
    Xirr: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/XirrRequest' } } } }
    MonteCarlo: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/MonteCarloRequest' } } } }
    PortfolioMonteCarlo: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PortfolioMonteCarloRequest' } } } }
    PostTaxReturn: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PostTaxReturnRequestV2' } } } }
    PostTaxReturnBatch: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PostTaxReturnBatchRequestV2' } } } }
    PortfolioOptimise: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PortfolioOptimiseRequest' } } } }
    Rebalance: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/RebalanceRequest' } } } }
    Chat: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ChatRequest' } } } }
    RegimeAdjust: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/RegimeAdjustRequest' } } } }
  responses:
    Object: { description: Successful structured response, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
    Message: { description: Successful operation, content: { application/json: { schema: { type: object, required: [message], properties: { message: { type: string } } } } } }
    AuthSession: { description: Authenticated session, content: { application/json: { schema: { $ref: '#/components/schemas/AuthSession' } } } }
    Profile: { description: Authoritative financial profile, content: { application/json: { schema: { $ref: '#/components/schemas/FinancialProfile' } } } }
    ProfileUpdate: { description: Atomically committed profile and canonical current recommendation state, content: { application/json: { schema: { $ref: '#/components/schemas/ProfileUpdateResponse' } } } }
    ProfileCandidate: { description: Short-lived non-authoritative precomputation handle, content: { application/json: { schema: { $ref: '#/components/schemas/ProfileCandidate' } } } }
    ProfileCompletion: { description: Atomically committed profile and recommendation, content: { application/json: { schema: { $ref: '#/components/schemas/ProfileCompletion' } } } }
    Recommendation: { description: Recommendation and audit identifiers, content: { application/json: { schema: { $ref: '#/components/schemas/RecommendationResponse' } } } }
    DeferredAdvisory: { description: Advisory explanation bound to its exact recommendation allocation state, content: { application/json: { schema: { $ref: '#/components/schemas/DeferredAdvisoryResponse' } } } }
    PlanReview: { description: Bounded read-only plan review, content: { application/json: { schema: { $ref: '#/components/schemas/PlanReviewResponse' } } } }
    PlanReviewRun: { description: Durable plan review run, content: { application/json: { schema: { $ref: '#/components/schemas/PlanReviewRun' } } } }
    UserIntentMandate: { description: Cryptographically bound AP2-inspired WealthGenie action authorization, content: { application/json: { schema: { $ref: '#/components/schemas/UserIntentMandate' } } } }
    ExecutionReceipt: { description: Tamper-evident authorized action execution receipt, content: { application/json: { schema: { $ref: '#/components/schemas/ExecutionReceipt' } } } }
    ExecutionResult: { description: Authorized action result and execution receipt, content: { application/json: { schema: { type: object, required: [receipt], properties: { receipt: { $ref: '#/components/schemas/ExecutionReceipt' }, response: { type: object, additionalProperties: true } } } } } }
    Goal: { description: Goal state, content: { application/json: { schema: { $ref: '#/components/schemas/Goal' } } } }
    GoalCreate:
      description: Created goal.
      content:
        application/json:
          schema:
            type: object
            required: [goal]
            properties:
              goal: { $ref: '#/components/schemas/Goal' }
            additionalProperties: false
    GoalUpdate:
      description: Updated goal.
      content:
        application/json:
          schema:
            type: object
            required: [success, goal]
            properties:
              success: { type: boolean, const: true }
              goal: { $ref: '#/components/schemas/Goal' }
            additionalProperties: false
    GoalAdvice:
      description: Goal advisory bound to the saved goal and source financial state.
      content:
        application/json:
          schema:
            type: object
            required: [goal, goalId, gemini_advice, operation_result]
            properties:
              goal: { $ref: '#/components/schemas/Goal' }
              goalId: { type: string }
              gemini_advice: { type: [string, 'null'] }
              operation_result:
                type: object
                required: [committed, goal_version, response_state]
                properties:
                  committed: { type: boolean, const: true }
                  goal_version: { type: integer, minimum: 1 }
                  response_state: { type: string, enum: [CURRENT, STALE] }
                additionalProperties: false
            additionalProperties: false
    GoalDelete:
      description: Deleted goal identity.
      content:
        application/json:
          schema:
            type: object
            required: [deleted, goalId]
            properties:
              deleted: { type: boolean, const: true }
              goalId: { type: string }
            additionalProperties: false
    TaxCalculation: { description: Authoritative tax calculation, content: { application/json: { schema: { $ref: '#/components/schemas/TaxCalculation' } } } }
    TaxComparison: { description: Authoritative regime comparison, content: { application/json: { schema: { $ref: '#/components/schemas/TaxComparison' } } } }
    Portfolio: { description: Server-authoritative portfolio decision, content: { application/json: { schema: { $ref: '#/components/schemas/PortfolioResult' } } } }
    Chat: { description: Assistant response, content: { application/json: { schema: { $ref: '#/components/schemas/ChatResponse' } } } }
    Health: { description: Service health, content: { application/json: { schema: { $ref: '#/components/schemas/HealthStatus' } } } }
    BadRequest: { description: Invalid request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    Unauthorized: { description: Authentication required or invalid, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    Forbidden: { description: Authenticated caller is not permitted, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    NotFound: { description: Resource not found, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    Conflict: { description: Request conflicts with current state, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    Error: { description: Canonical API error envelope, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    PreconditionRequired: { description: A version precondition is required, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    Unprocessable: { description: A well-formed request cannot be processed under current policy, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    Unavailable: { description: Service or dependency unavailable, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    BadGateway: { description: An upstream service could not provide valid output, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
  schemas:
    Error:
      type: object
      required: [error, message, code, request_id]
      properties:
        error: { type: string, description: Backward-compatible summary }
        message: { type: string }
        code: { type: string, pattern: '^[A-Z0-9_]+$' }
        request_id: { type: [string, 'null'] }
        details: { oneOf: [{ type: array, items: { type: string } }, { type: object, additionalProperties: true }] }
    User:
      type: object
      required: [id, name, email, role]
      properties: { id: { type: string }, name: { type: string }, email: { type: string, format: email }, mobile: { type: [string, 'null'] }, role: { type: string }, createdAt: { type: string, format: date-time } }
    AuthSession:
      type: object
      required: [csrfToken, user]
      properties:
        csrfToken: { type: string, writeOnly: true }
        user: { $ref: '#/components/schemas/User' }
        token: { type: string, writeOnly: true, description: Development-only compatibility field; never emitted in production. }
    RegisterRequest:
      type: object
      additionalProperties: false
      required: [name, email, password]
      properties: { name: { type: string, minLength: 2, maxLength: 100 }, email: { type: string, format: email }, password: { type: string, minLength: 8, maxLength: 128, writeOnly: true }, mobile: { type: string, minLength: 10, maxLength: 15 } }
    LoginRequest:
      type: object
      additionalProperties: false
      required: [email, password]
      description: Login accepts any historically valid non-empty password; registration enforces the stronger password policy.
      properties: { email: { type: string, format: email }, password: { type: string, minLength: 1, writeOnly: true } }
    FinancialProfileFields:
      type: object
      required: [monthly_take_home, monthly_savings, age, risk_tolerance, investment_goals, investment_horizon_years]
      properties:
        monthly_take_home: { type: number, exclusiveMinimum: 0, maximum: 100000000 }
        monthly_savings: { type: number, exclusiveMinimum: 0, maximum: 100000000, description: Must be less than monthly_take_home. }
        age: { type: integer, minimum: 18, maximum: 80 }
        risk_tolerance: { type: string, enum: [Conservative, Moderate, Aggressive] }
        sold_property_proceeds: { type: [number, 'null'], minimum: 0, maximum: 10000000000, description: Optional historical context only; never deployable capital. Null means not provided. }
        has_lump_sum: { type: [boolean, 'null'], description: Optional. Null means not provided. }
        lump_sum_amount: { type: [number, 'null'], minimum: 0, maximum: 10000000000, description: Positive when has_lump_sum is true, 0 when false, and null when unknown. }
        liquid_savings: { type: [number, 'null'], minimum: 0, maximum: 1000000000, description: Optional; null means unknown and is never treated as zero. }
        emi_burden_pct: { type: [number, 'null'], minimum: 0, maximum: 100, description: Optional; null means unknown and is never treated as zero. }
        financial_dependents: { type: [integer, 'null'], minimum: 0, maximum: 15, description: Optional; null means not provided. }
        emergency_fund_months: { type: [number, 'null'], minimum: 0, maximum: 120, description: Optional; null means unknown and is never treated as zero. }
        investment_goals: { type: array, minItems: 1, maxItems: 4, uniqueItems: true, items: { type: string, enum: [Retirement, Wealth Growth, Tax Saving, Emergency Fund] } }
        investment_horizon_years: { type: integer, minimum: 1, maximum: 30 }
    FinancialProfileInput:
      allOf: [{ $ref: '#/components/schemas/FinancialProfileFields' }]
      unevaluatedProperties: false
    FinancialProfileCompletion:
      allOf:
        - { $ref: '#/components/schemas/FinancialProfileFields' }
        - type: object
          properties:
            candidateId: { type: string, format: uuid }
      unevaluatedProperties: false
    ProfileCandidate:
      type: object
      required: [candidateId, status, expiresInMs, profileFingerprint, modelVersion, recommendationPolicyVersion, financialProfileSchemaVersion, regulatoryRuleVersion]
      properties:
        candidateId: { type: string, format: uuid }
        status: { type: string, enum: [READY] }
        expiresInMs: { type: integer, minimum: 0 }
        profileFingerprint: { type: string, pattern: '^[a-f0-9]{64}$' }
        modelVersion: { type: string }
        recommendationPolicyVersion: { type: string }
        financialProfileSchemaVersion: { type: string }
        regulatoryRuleVersion: { type: string }
        marketContext: { type: object, additionalProperties: true }
    ProfileCompletion:
      type: object
      required: [profile, recommendation, completion]
      properties:
        profile: { $ref: '#/components/schemas/FinancialProfile' }
        recommendation: { $ref: '#/components/schemas/RecommendationResponse' }
        completion: { type: object, additionalProperties: true }
        operation_result: { $ref: '#/components/schemas/CommittedOperationResult' }
    ProfileUpdateResponse:
      type: object
      required: [profile, recommendation]
      properties:
        profile: { $ref: '#/components/schemas/FinancialProfile' }
        recommendation: { $ref: '#/components/schemas/RecommendationResponse' }
        operation_result: { $ref: '#/components/schemas/CommittedOperationResult' }
    FinancialProfileUpdate:
      allOf: [{ $ref: '#/components/schemas/FinancialProfileFields' }, { type: object, required: [version], properties: { version: { type: integer, minimum: 1 } } }]
      unevaluatedProperties: false
    FinancialProfile:
      allOf:
        - { $ref: '#/components/schemas/FinancialProfileFields' }
        - type: object
          required: [profileId, version, monthly_investment_capacity, one_time_investment_capacity, profile_schema_version, risk_capacity_score, risk_capacity_level, final_suitability_level, suitability_reason_codes]
          properties:
            profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
            version: { type: integer, minimum: 1 }
            monthly_investment_capacity: { type: number, minimum: 0 }
            one_time_investment_capacity: { type: [number, 'null'], minimum: 0 }
            profile_schema_version: { type: string }
            risk_capacity_score: { type: number, minimum: 0, maximum: 100 }
            risk_capacity_level: { type: integer, minimum: 1, maximum: 5 }
            final_suitability_risk: { type: string }
            final_suitability_level: { type: integer, minimum: 1, maximum: 5 }
            suitability_reason_codes: { type: array, items: { type: string } }
      unevaluatedProperties: false
    RecommendationRequest: { type: object, additionalProperties: false, required: [profileId], properties: { profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' } } }
    PlanReviewRequest: { type: object, additionalProperties: false, required: [profileId], properties: { profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' } } }
    PlanReviewResponse:
      type: object
      required: [version, runId, status, recommendedAction, summary, findings, freshness, goals, evidence, provider]
      properties:
        version: { type: string, const: plan-review-1.0.0 }
        runId: { type: string, format: uuid }
        status: { type: string, enum: [COMPLETED, FAILED, FEATURE_UNAVAILABLE] }
        recommendedAction: { type: string, enum: [NONE, REVIEW_PROFILE, RECOMPUTE_PLAN, REVIEW_GOALS, INSUFFICIENT_EVIDENCE] }
        summary: { type: string }
        findings: { type: array, items: { type: object, additionalProperties: true } }
        freshness: { type: object, additionalProperties: true }
        goals: { type: object, additionalProperties: true }
        evidence: { type: object, additionalProperties: true }
        provider: { type: object, additionalProperties: true }
      additionalProperties: false
    PlanReviewRun:
      type: object
      required: [runId, status, agentVersion, graphVersion]
      properties:
        runId: { type: string, format: uuid }
        status: { type: string, enum: [QUEUED, RUNNING, WAITING_FOR_APPROVAL, COMPLETED, FAILED, CANCELLED, SUPERSEDED, BUDGET_EXCEEDED, FEATURE_UNAVAILABLE] }
        profileId: { type: [string, 'null'] }
        recommendationId: { type: [string, 'null'] }
        planReviewSnapshotHash: { type: [string, 'null'], pattern: '^[a-f0-9]{64}$' }
        currentStateMatch: { type: [boolean, 'null'] }
        recommendedAction: { type: string }
        result: { oneOf: [{ $ref: '#/components/schemas/PlanReviewResponse' }, { type: 'null' }] }
        progress: { type: object, additionalProperties: true }
        currentNode: { type: [string, 'null'] }
        attempt: { type: integer, minimum: 0 }
        maxAttempts: { type: integer, minimum: 1, maximum: 2 }
        tokenUsage: { type: integer, minimum: 0, maximum: 5200 }
        agentVersion: { type: string }
        graphVersion: { type: string }
        queuedAt: { type: [string, 'null'], format: date-time }
        startedAt: { type: [string, 'null'], format: date-time }
        completedAt: { type: [string, 'null'], format: date-time }
        failure: { type: [object, 'null'], additionalProperties: true }
        approval: { type: [object, 'null'], additionalProperties: true }
      additionalProperties: true
    UserIntentMandate:
      type: object
      required: [mandateId, version, action, agentType, runId, profileId, financialSnapshotHash, recommendationFingerprint, policyVersion, actionPayloadHash, constraints, issuedAt, expiresAt, singleUse, approvalMethod, status, mandateHash]
      properties:
        mandateId: { type: string, format: uuid }
        version: { type: string, const: user-intent-mandate-1.0.0 }
        action: { type: string, const: APPROVE_RECOMPUTE }
        agentType: { type: string, const: PLAN_REVIEW }
        agentVersion: { type: string }
        runId: { type: string, format: uuid }
        profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
        recommendationId: { type: [string, 'null'] }
        financialSnapshotHash: { type: string, pattern: '^[a-f0-9]{64}$' }
        recommendationFingerprint: { type: string, pattern: '^[a-f0-9]{64}$' }
        policyVersion: { type: string }
        actionPayloadHash: { type: string, pattern: '^[a-f0-9]{64}$' }
        constraints: { type: object, additionalProperties: false, required: [maxAgeSeconds, allowedAgentType, resourceVersion, noFinancialMutationByAgent], properties: { maxAgeSeconds: { type: integer, minimum: 1, maximum: 900 }, allowedAgentType: { type: string, const: PLAN_REVIEW }, resourceVersion: { type: integer, minimum: 1 }, noFinancialMutationByAgent: { type: boolean, const: true } } }
        issuedAt: { type: string, format: date-time }
        notBefore: { type: string, format: date-time }
        expiresAt: { type: string, format: date-time }
        singleUse: { type: boolean, const: true }
        approvalMethod: { type: string, enum: [DEVELOPMENT, WEBAUTHN] }
        status: { type: string, enum: [DRAFT, PENDING_USER_VERIFICATION, AUTHORIZED, EXECUTING, EXECUTED, REJECTED, EXPIRED, REVOKED, FAILED] }
        mandateHash: { type: string, pattern: '^[a-f0-9]{64}$' }
        signatureMetadata: { type: [object, 'null'], additionalProperties: true }
        approval:
          type: [object, 'null']
          additionalProperties: false
          properties:
            method: { type: [string, 'null'], enum: [DEVELOPMENT, WEBAUTHN, null] }
            verifiedAt: { type: [string, 'null'], format: date-time }
            credentialId: { type: [string, 'null'], maxLength: 512 }
            challengeHash: { type: [string, 'null'], pattern: '^[a-f0-9]{64}$' }
            authenticatorCounter: { type: [integer, 'null'], minimum: 0 }
            credentialDeviceType: { type: [string, 'null'], enum: [singleDevice, multiDevice, null] }
            credentialBackedUp: { type: [boolean, 'null'] }
        receiptId: { type: [string, 'null'] }
      additionalProperties: false
    MandateApprovalAssertion:
      type: object
      additionalProperties: false
      required: [method, mandateId, mandateHash]
      properties:
        method: { type: string, enum: [DEVELOPMENT, WEBAUTHN] }
        mandateId: { type: string, format: uuid }
        mandateHash: { type: string, pattern: '^[a-f0-9]{64}$' }
        credentialId: { type: [string, 'null'] }
        response: { type: [object, 'null'], additionalProperties: true }
    PasskeyRegistrationResponse:
      type: object
      additionalProperties: false
      required: [id, rawId, type, response, clientExtensionResults]
      properties:
        id: { type: string }
        rawId: { type: string }
        type: { type: string, const: public-key }
        response: { type: object, additionalProperties: true }
        clientExtensionResults: { type: object, additionalProperties: true }
    ExecutionReceipt:
      type: object
      required: [receiptId, version, mandateId, action, status, executedByServiceIdentity, runId, resourceId, beforeSnapshotHash, startedAt, completedAt, policyDecisionId, policyVersion, mandateHash, resultMetadata, receiptHash, signatureMetadata]
      properties:
        receiptId: { type: string, format: uuid }
        version: { type: string, const: execution-receipt-1.0.0 }
        mandateId: { type: string, format: uuid }
        action: { type: string, const: APPROVE_RECOMPUTE }
        status: { type: string, const: EXECUTED }
        executedByServiceIdentity: { type: string }
        runId: { type: string, format: uuid }
        resourceId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
        beforeSnapshotHash: { type: string, pattern: '^[a-f0-9]{64}$' }
        afterSnapshotHash: { type: [string, 'null'], pattern: '^[a-f0-9]{64}$' }
        startedAt: { type: string, format: date-time }
        completedAt: { type: string, format: date-time }
        policyDecisionId: { type: string }
        policyVersion: { type: string }
        mandateHash: { type: string, pattern: '^[a-f0-9]{64}$' }
        previousReceiptHash: { type: [string, 'null'], pattern: '^[a-f0-9]{64}$' }
        resultMetadata: { type: object, additionalProperties: true }
        receiptHash: { type: string, pattern: '^[a-f0-9]{64}$' }
        signatureMetadata: { type: object, additionalProperties: false, required: [algorithm, keyId, signature, environment], properties: { algorithm: { type: string, const: Ed25519 }, keyId: { type: string }, signature: { type: string }, environment: { type: string, enum: [development, configured] } } }
      additionalProperties: false
    CommittedOperationResult:
      oneOf:
        - type: object
          required: [committed, generated_recommendation_id, superseded_before_response]
          properties:
            committed: { type: boolean, const: true }
            generated_recommendation_id: { type: string, pattern: '^[0-9a-fA-F]{24}$', description: Recommendation generation created by the idempotent operation; it is not necessarily the current recommendation. }
            superseded_before_response: { type: boolean, const: true }
          additionalProperties: false
        - type: object
          required: [committed, generated_recommendation_id, generated_allocation_revision, generated_allocation_revision_id, superseded_before_response]
          properties:
            committed: { type: boolean, const: true }
            generated_recommendation_id: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
            generated_allocation_revision: { type: integer, minimum: 1 }
            generated_allocation_revision_id: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
            superseded_before_response: { type: boolean }
          additionalProperties: false
    RecommendationResponse:
      type: object
      required: [recommendationId, recommendation_id, profileId, profile_version, profile_input_hash, recommendation_fingerprint, response_state, allocation_revision, allocation_revision_id, previous_allocation_revision, previous_allocation_revision_id, portfolio_fingerprint, calculation_freshness, state_provenance, current_allocation_source, audit_id, audit_hash, instruments, model_version, recommendation_policy_version, regulatory_rule_version, return_assumption_version, return_assumption_hash, return_assumption_source, financial_profile_schema_version, return_basis, final_risk_tier]
      properties:
        recommendationId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
        recommendation_id: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
        profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
        profile_version: { type: integer, minimum: 1 }
        profile_input_hash: { type: string, pattern: '^[a-f0-9]{64}$' }
        recommendation_fingerprint: { type: string, pattern: '^[a-f0-9]{64}$' }
        response_state: { type: string, const: CURRENT }
        operation_result: { $ref: '#/components/schemas/CommittedOperationResult', description: Present only when this operation's committed generation was superseded before response construction. }
        audit_id: { type: string }
        audit_hash: { type: string, pattern: '^[a-f0-9]{64}$' }
        instruments: { type: array, minItems: 1, items: { type: object, additionalProperties: true } }
        generation_instruments: { type: array, items: { type: object, additionalProperties: true } }
        allocation_revision: { type: integer, minimum: 1 }
        allocation_revision_id: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
        previous_allocation_revision: { type: [integer, 'null'], minimum: 1 }
        previous_allocation_revision_id: { type: [string, 'null'], pattern: '^[0-9a-fA-F]{24}$' }
        portfolio_fingerprint: { type: string, pattern: '^[a-f0-9]{64}$' }
        calculation_freshness: { $ref: '#/components/schemas/FinancialStateFreshness' }
        state_provenance: { $ref: '#/components/schemas/FinancialStateProvenance' }
        confidence_scores: { type: object, additionalProperties: true }
        market_adjustment: { oneOf: [{ $ref: '#/components/schemas/MarketAdjustmentMetadata' }, { type: 'null' }] }
        current_allocation_source: { type: string, enum: [ORIGINAL_RECOMMENDATION, MARKET_CONTEXT_ADJUSTED, USER_REBALANCED] }
        model_version: { type: string }
        ml_fallback: { type: boolean }
        optional_profile_fields_unknown: { type: array, items: { type: string } }
        ml_input_fields_unknown: { type: array, items: { type: string } }
        recommendation_policy_version: { type: string }
        regulatory_rule_version: { type: string }
        return_assumption_version: { type: string }
        return_assumption_hash: { type: string, pattern: '^[a-f0-9]{64}$' }
        financial_profile_schema_version: { type: string }
        return_basis: { type: string, const: PRE_TAX_NOMINAL }
        final_risk_tier: { type: string }
        excluded_due_to_eligibility: { type: array, items: { type: object, additionalProperties: true } }
      additionalProperties: true
    FinancialStateFreshness:
      type: object
      required: [fresh, reasonCodes, profilePresent, recommendationPresent, modelVersion, expectedProfileHash, observedProfileHash, expectedProfileVersion, observedProfileVersion, allocationRevision, currentAllocationSource, observedRegulatoryVersion, currentRegulatoryVersion, policyVersion, observedRecommendationPolicyVersion, assumptionVersion, assumptionHash, assumptionSource]
      properties:
        fresh: { type: boolean, const: true }
        reasonCodes: { type: array, maxItems: 0, items: { type: string } }
        profilePresent: { type: boolean, const: true }
        recommendationPresent: { type: boolean, const: true }
        modelVersion: { type: string }
        expectedProfileHash: { type: string, pattern: '^[a-f0-9]{64}$' }
        observedProfileHash: { type: string, pattern: '^[a-f0-9]{64}$' }
        expectedProfileVersion: { type: integer, minimum: 1 }
        observedProfileVersion: { type: integer, minimum: 1 }
        allocationRevision: { type: integer, minimum: 1 }
        currentAllocationSource: { type: string, enum: [ORIGINAL_RECOMMENDATION, MARKET_CONTEXT_ADJUSTED, USER_REBALANCED] }
        observedRegulatoryVersion: { type: string }
        currentRegulatoryVersion: { type: string }
        policyVersion: { type: string }
        observedRecommendationPolicyVersion: { type: string }
        assumptionVersion: { type: string }
        assumptionHash: { type: string, pattern: '^[a-f0-9]{64}$' }
        assumptionSource: { type: string }
      additionalProperties: true
    FinancialStateProvenance:
      type: object
      required: [status, stateId, recommendationId, allocationSource, allocationRevision, allocationRevisionId, previousAllocationRevision, previousAllocationRevisionId, profileVersion, profileInputHash, portfolioFingerprint, recommendationFingerprint, recommendationPolicyVersion, regulatoryRuleVersion, returnAssumptionVersion, returnAssumptionHash, returnAssumptionSource]
      properties:
        status: { type: string, const: PERSISTED_REVISION }
        stateId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
        recommendationId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
        allocationSource: { type: string, enum: [ORIGINAL_RECOMMENDATION, MARKET_CONTEXT_ADJUSTED, USER_REBALANCED] }
        allocationRevision: { type: integer, minimum: 1 }
        allocationRevisionId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
        previousAllocationRevision: { type: [integer, 'null'], minimum: 1 }
        previousAllocationRevisionId: { type: [string, 'null'], pattern: '^[0-9a-fA-F]{24}$' }
        profileVersion: { type: integer, minimum: 1 }
        profileInputHash: { type: string, pattern: '^[a-f0-9]{64}$' }
        portfolioFingerprint: { type: string, pattern: '^[a-f0-9]{64}$' }
        recommendationFingerprint: { type: string, pattern: '^[a-f0-9]{64}$' }
        recommendationPolicyVersion: { type: string }
        regulatoryRuleVersion: { type: string }
        returnAssumptionVersion: { type: string }
        returnAssumptionHash: { type: string, pattern: '^[a-f0-9]{64}$' }
        returnAssumptionSource: { type: string }
      additionalProperties: true
    DeferredAdvisoryResponse:
      type: object
      required: [recommendationId, profileId, profile_version, profile_input_hash, recommendation_fingerprint, recommendation_policy_version, regulatory_rule_version, return_assumption_hash, allocation_revision, allocation_revision_id, portfolio_fingerprint, advisory_text, advisory_explanation]
      properties:
        recommendationId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
        profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
        profile_version: { type: integer, minimum: 1 }
        profile_input_hash: { type: string, pattern: '^[a-f0-9]{64}$' }
        recommendation_fingerprint: { type: string, pattern: '^[a-f0-9]{64}$' }
        recommendation_policy_version: { type: string }
        regulatory_rule_version: { type: string }
        return_assumption_hash: { type: string, pattern: '^[a-f0-9]{64}$' }
        allocation_revision: { type: integer, minimum: 1 }
        allocation_revision_id: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
        portfolio_fingerprint: { type: string, pattern: '^[a-f0-9]{64}$' }
        advisory_text: { type: [string, 'null'] }
        advisory_explanation:
          type: object
          required: [recommendationId, allocation_revision, allocation_revision_id, portfolio_fingerprint]
          properties:
            recommendationId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
            allocation_revision: { type: integer, minimum: 1 }
            allocation_revision_id: { type: string, pattern: '^[0-9a-fA-F]{24}$' }
            portfolio_fingerprint: { type: string, pattern: '^[a-f0-9]{64}$' }
            status: { type: string }
          additionalProperties: true
      additionalProperties: true
    AuditMetadata:
      type: object
      properties: { _id: { type: string }, recommendationId: { type: string }, modelVersion: { type: string }, ruleVersion: { type: string }, citedRagChunkIds: { type: array, items: { type: string } }, previous_hash: { type: [string, 'null'] }, record_hash: { type: string }, hash_algorithm: { type: string }, schema_version: { type: string }, correlationId: { type: string }, createdAt: { type: string, format: date-time } }
      additionalProperties: true
    AuditVerification: { type: object, required: [valid], properties: { valid: { type: boolean }, checked: { type: integer }, reason: { type: string } } }
    RecommendationWeightsRequest: { type: object, additionalProperties: false, required: [profileId, recommendationId, expectedAllocationRevision, expectedPortfolioFingerprint, weights], properties: { profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }, recommendationId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }, expectedAllocationRevision: { type: integer, minimum: 1, description: Optimistic-concurrency revision expected by the client; the server remains authoritative. }, expectedPortfolioFingerprint: { type: string, pattern: '^[a-f0-9]{64}$', description: Optimistic-concurrency portfolio fingerprint expected by the client. }, weights: { type: object, minProperties: 1, maxProperties: 30, additionalProperties: { type: number, minimum: 0, maximum: 1 }, description: Every recommended instrument must be present and weights must sum to exactly 1. } } }
    GoalCreateRequest:
      type: object
      additionalProperties: false
      required: [goal_name, target_amount, target_date, current_savings, profileId, priority]
      properties: { goal_name: { type: string, minLength: 2, maxLength: 100 }, target_amount: { type: number, minimum: 1000, maximum: 10000000000 }, target_date: { type: string, format: date }, current_savings: { type: number, minimum: 0, maximum: 10000000000 }, profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }, priority: { type: string, enum: [Critical, High, Medium, Low] } }
    GoalUpdateRequest: { type: object, additionalProperties: false, minProperties: 2, required: [expectedVersion], properties: { expectedVersion: { type: integer, minimum: 1 }, target_amount: { type: number, minimum: 1000, maximum: 10000000000 }, current_savings: { type: number, minimum: 0, maximum: 10000000000 }, priority: { type: string, enum: [Critical, High, Medium, Low] } } }
    GoalSimulationRequest: { type: object, additionalProperties: false, required: [monthly_contribution], properties: { monthly_contribution: { type: number, exclusiveMinimum: 0, maximum: 10000000 } } }
    Goal:
      type: object
      required: [goalId, version, goal_name, target_amount, target_date, current_savings, priority, calculation_freshness, advisory_freshness, advice_stale, source_provenance]
      properties:
        _id: { type: string }
        goalId: { type: string, minLength: 1 }
        profileId: { type: string }
        version: { type: integer, minimum: 1 }
        goal_name: { type: string, minLength: 2, maxLength: 100 }
        target_amount: { type: number, minimum: 1000 }
        inflation_adjusted_target: { type: [number, 'null'] }
        target_date: { type: string, format: date-time }
        current_savings: { type: number, minimum: 0 }
        recommended_sip: { type: [number, 'null'] }
        simulated_monthly_contribution: { type: [number, 'null'] }
        recommended_instrument: { type: [string, 'null'] }
        probability_of_success: { type: [number, 'null'], minimum: 0, maximum: 1 }
        gap_amount: { type: [number, 'null'] }
        priority: { type: string, enum: [Critical, High, Medium, Low] }
        status: { type: [string, 'null'], enum: [on_track, at_risk, off_track, null] }
        monte_carlo_summary: { type: [object, 'null'], additionalProperties: true }
        chart_data: { type: array, items: { type: object, additionalProperties: true } }
        chartData: { type: array, items: { type: object, additionalProperties: true } }
        mc_computed_at: { type: [string, 'null'], format: date-time }
        years_remaining: { type: [number, 'null'] }
        simulation_classification: { type: [string, 'null'], enum: [PROFILE_GROUNDED_GOAL_PLAN, NON_RECOMMENDATION_GOAL_WHAT_IF, null] }
        return_basis: { type: [string, 'null'], enum: [PRE_TAX_NOMINAL, null] }
        return_data_class: { type: [string, 'null'], enum: [MODEL_ASSUMPTION, null] }
        return_assumption_version: { type: [string, 'null'] }
        return_assumption_source: { type: [string, 'null'], enum: [WEALTHGENIE_MODEL_POLICY, null] }
        observed_market_fact: { type: [boolean, 'null'] }
        provider_forecast: { type: [boolean, 'null'] }
        inflation_assumption: { type: [number, 'null'] }
        sourceRecommendationId: { type: [string, 'null'] }
        sourceAllocationRevision: { type: [integer, 'null'] }
        sourceAllocationRevisionId: { type: [string, 'null'] }
        sourceProfileInputHash: { type: [string, 'null'], pattern: '^[a-f0-9]{64}$' }
        sourceProfileVersion: { type: [integer, 'null'] }
        sourceModelVersion: { type: [string, 'null'] }
        sourceRecommendationPolicyVersion: { type: [string, 'null'] }
        sourceRegulatoryRuleVersion: { type: [string, 'null'] }
        sourceReturnAssumptionVersion: { type: [string, 'null'] }
        sourceReturnAssumptionHash: { type: [string, 'null'], pattern: '^[a-f0-9]{64}$' }
        sourceReturnAssumptionSource: { type: [string, 'null'] }
        sourceRecommendationFingerprint: { type: [string, 'null'], pattern: '^[a-f0-9]{64}$' }
        sourcePortfolioFingerprint: { type: [string, 'null'], pattern: '^[a-f0-9]{64}$' }
        sourceGoalCalculationInputFingerprint: { type: [string, 'null'], pattern: '^[a-f0-9]{64}$' }
        sourceGoalCalculationPolicyVersion: { type: [string, 'null'] }
        calculationFreshness: { type: [object, 'null'], additionalProperties: true }
        calculation_freshness:
          type: object
          required: [fresh, reasonCodes]
          properties:
            fresh: { type: boolean }
            reasonCodes: { type: array, items: { type: string } }
          additionalProperties: true
        advisory_freshness:
          type: object
          required: [fresh, reasonCodes]
          properties:
            fresh: { type: boolean }
            reasonCodes: { type: array, items: { type: string } }
          additionalProperties: true
        advice_stale: { type: boolean }
        source_provenance: { type: object, additionalProperties: true }
        gemini_advice: { type: [string, 'null'], maxLength: 2000 }
        createdAt: { type: [string, 'null'], format: date-time }
        updatedAt: { type: [string, 'null'], format: date-time }
      additionalProperties: false
    Instrument:
      type: object
      additionalProperties: false
      required: [id, name]
      properties:
        id: { type: string, minLength: 1 }
        name: { type: string, minLength: 1 }
        abbr: { type: string }
        type: { type: string }
        category: { type: string }
        cat: { type: string }
        subCategory: { type: string }
        assetClass: { type: string }
        provider: { type: string }
        expectedReturn: { type: number }
        rate: { type: number }
        returnRange: { type: object, additionalProperties: false, properties: { min: { type: number }, max: { type: number } } }
        interestRate: { type: number }
        interestRateSenior: { type: number }
        returns1yr: { type: number }
        returns3yr: { type: number }
        returns5yr: { type: number }
        riskLevel: { type: [string, number, boolean, object, array, 'null'] }
        risk: { type: number }
        riskLabel: { type: string }
        volatility: { type: number }
        liquidityScore: { type: number }
        lockIn: { type: number }
        lockInYears: { type: number }
        taxType: { type: string }
        taxEfficiencyScore: { type: number }
        expenseRatio: { type: number }
        minMonthlyInvestment: { type: number }
        maxAnnualInvestment: { type: number }
        minInvestment: { type: number }
        idealHorizon: { type: object, additionalProperties: false, properties: { min: { type: number }, max: { type: number } } }
        goalTags: { type: array, items: { type: string } }
        maturityYears: { type: number }
        eligibility:
          type: object
          additionalProperties: false
          properties:
            minAge: { type: number }
            maxAge: { type: number }
            minAnnualIncome: { type: number }
            minMonthlySavings: { type: number }
            requiresDemat: { type: boolean }
            hasGirlChild: { type: boolean }
            requires_daughter_under_10: { type: boolean }
            notes: { type: string }
        nav: { type: number }
        aumCr: { type: number }
        exitLoad: { type: string }
        sebiRating: { type: string }
        trackingError: { type: number }
        underlyingIndex: { type: string }
        exchange: { type: string }
        issuer: { type: string }
        sovereignGuarantee: { type: boolean }
        tdsApplicable: { type: boolean }
        prematureWithdrawalPenalty: { type: string }
        color: { type: string }
        desc: { type: string }
    InstrumentPage: { type: object, additionalProperties: false, required: [instruments, total, page, pageSize, totalPages], properties: { instruments: { type: array, items: { $ref: '#/components/schemas/Instrument' } }, total: { type: integer, minimum: 0 }, page: { type: integer, minimum: 1 }, pageSize: { type: integer, minimum: 1 }, totalPages: { type: integer, minimum: 0 } } }
    AuditListResponse: { type: object, required: [status, total, count, skip, limit, records], properties: { status: { type: string, const: success }, total: { type: integer, minimum: 0 }, count: { type: integer, minimum: 0 }, skip: { type: integer, minimum: 0 }, limit: { type: integer, minimum: 1, maximum: 100 }, records: { type: array, items: { $ref: '#/components/schemas/AuditMetadata' } } }, additionalProperties: false }
    ChatHistoryResponse: { type: object, additionalProperties: false, required: [conversations], properties: { conversations: { type: array, items: { type: object, required: [session_id, messages], properties: { session_id: { type: string }, messages: { type: array, items: { type: object, additionalProperties: true } }, message_count: { type: integer }, created_at: { type: string }, updated_at: { type: string } }, additionalProperties: true } } } }
    MarketContextResponse:
      type: object
      required: [status, evaluatedAt, marketSnapshot]
      properties:
        status: { type: string, enum: [MARKET_CONTEXT_AVAILABLE, MARKET_CONTEXT_UNAVAILABLE] }
        context: { type: [string, 'null'], enum: [NORMAL, CAUTIOUS, HIGH_VOLATILITY, RISK_OFF, MARKET_CONTEXT_UNAVAILABLE, null] }
        policyVersion: { type: [string, 'null'] }
        evaluatedAt: { type: string, format: date-time }
        marketSnapshot: { $ref: '#/components/schemas/MarketContextSnapshot' }
      additionalProperties: true
    MarketAdjustmentMetadata:
      type: object
      required: [status, applied, actualTotalTiltPct, reasonCodes, suitabilityValidation, concentrationValidation]
      properties:
        status: { type: string, enum: [APPLIED, NOT_APPLIED, UNAVAILABLE] }
        applied: { type: boolean }
        contextStatus: { type: string }
        context: { type: [string, 'null'] }
        policyVersion: { type: [string, 'null'] }
        observedAt: { type: [string, 'null'], format: date-time }
        evaluatedAt: { type: [string, 'null'], format: date-time }
        actualTotalTiltPct: { type: number, minimum: 0 }
        reasonCodes: { type: array, items: { type: string } }
        suitabilityValidation: { type: string }
        concentrationValidation: { type: string }
        currentAllocationSource: { type: string, enum: [ORIGINAL_RECOMMENDATION, MARKET_CONTEXT_ADJUSTED, USER_REBALANCED] }
        supersededByManualRebalanceAt: { type: [string, 'null'], format: date-time }
      additionalProperties: true
    MarketContextSnapshot:
      type: object
      required: [schemaVersion, status, availability, providerStatus, observedFacts, derivedFacts, policyOutput, provenance, marketSession]
      properties:
        schemaVersion: { type: string, const: market-snapshot-1.0.0 }
        status: { type: string, enum: [CURRENT, MARKET_CLOSED, LAST_AVAILABLE, STALE, PARTIAL_DATA, UNAVAILABLE] }
        availability: { type: string, enum: [AVAILABLE, PARTIAL, UNAVAILABLE] }
        providerStatus: { type: object, additionalProperties: true }
        observedFacts: { type: array, items: { $ref: '#/components/schemas/MarketFact' } }
        derivedFacts: { type: array, items: { $ref: '#/components/schemas/MarketFact' } }
        policyOutput: { type: object, required: [semanticClass, dataClass, status, reasonCodes], additionalProperties: true, properties: { semanticClass: { type: string, const: POLICY_OUTPUT }, dataClass: { type: string, const: POLICY_OUTPUT }, status: { type: string }, reasonCodes: { type: array, items: { type: string } } } }
        freshness: { type: [object, 'null'], additionalProperties: true }
        provenance: { type: object, additionalProperties: true }
        observedAt: { type: [string, 'null'], format: date-time }
        evaluatedAt: { type: [string, 'null'], format: date-time }
        marketSession: { type: object, required: [status], additionalProperties: true, properties: { status: { type: string, enum: [MARKET_OPEN, MARKET_CLOSED, MARKET_HOLIDAY, UNKNOWN] } } }
      additionalProperties: true
    MarketFact:
      type: object
      required: [key, value, dataClass, semanticClass, availabilityStatus]
      properties:
        key: { type: string }
        value: { type: [number, 'null'] }
        unit: { type: [string, 'null'] }
        dataClass: { type: [string, 'null'], description: Provider fact class such as LIVE or DAILY; null for derived features. }
        semanticClass: { type: string, enum: [OBSERVED, DERIVED] }
        availabilityStatus: { type: string, enum: [AVAILABLE, UNAVAILABLE] }
      additionalProperties: true
    TaxCalculationContext: { type: object, additionalProperties: false, required: [annualGrossIncome, incomeSource, regime, fiscalYear], properties: { fiscalYear: { type: string, pattern: '^FY\d{4}-\d{2}$', description: Fiscal year used by the server-owned tax policy; FY2025-26 maps to AY2026-27. }, incomeSource: { type: string, enum: [salary, pension, family_pension, business, other] }, annualGrossIncome: { type: number, minimum: 0, maximum: 1000000000 }, regime: { type: string, enum: [new, old] }, userAge: { type: integer, minimum: 18, maximum: 120, description: Required for old-regime calculations; persisted profile age is authoritative in WTI. }, deductions: { type: object, additionalProperties: true }, illustrativePrincipal: { type: number, exclusiveMinimum: 0, maximum: 1000000000 }, holdingPeriodMonths: { type: number, minimum: 0, maximum: 1200 }, section112AExemptionUsed: { type: number, minimum: 0, maximum: 125000 } } }
    RankInstrumentsRequest: { type: object, additionalProperties: false, required: [profileId, parentInstrumentId], properties: { profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }, parentInstrumentId: { type: string, maxLength: 50 }, taxCalculationContext: { $ref: '#/components/schemas/TaxCalculationContextV2' } } }
    ProjectionRequest: { type: object, additionalProperties: false, required: [profileId, instruments, monthly_investment, years], properties: { profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }, instruments: { type: array, minItems: 1, maxItems: 10, items: { type: string, maxLength: 50 } }, monthly_investment: { type: number, exclusiveMinimum: 0, maximum: 10000000 }, years: { type: array, minItems: 1, maxItems: 10, uniqueItems: true, items: { type: integer, minimum: 1, maximum: 30 } } } }
    ProjectionComparisonRequest: { type: object, additionalProperties: false, required: [monthlyInvestment, annualReturnRate, benchmarkRate, inflationRate, years], properties: { monthlyInvestment: { type: number, exclusiveMinimum: 0, maximum: 10000000 }, annualReturnRate: { type: number, exclusiveMinimum: -1, maximum: 1 }, benchmarkRate: { type: number, exclusiveMinimum: -1, maximum: 1 }, inflationRate: { type: number, minimum: 0, maximum: 1 }, years: { type: integer, minimum: 1, maximum: 50 } } }
    AllocationSplitRequest: { type: object, additionalProperties: false, required: [monthlyInvestment, equityPct], properties: { monthlyInvestment: { type: number, exclusiveMinimum: 0, maximum: 10000000 }, equityPct: { type: number, minimum: 0, maximum: 100 } } }
    PortfolioWeights: { type: object, minProperties: 1, maxProperties: 30, additionalProperties: { type: number, minimum: 0, maximum: 1 }, description: Weights must sum to exactly 1. }
    CustomPortfolioProjectionRequest: { type: object, additionalProperties: false, required: [profileId, allocations, years], properties: { profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }, allocations: { $ref: '#/components/schemas/PortfolioWeights' }, years: { type: integer, minimum: 1, maximum: 30 } } }
    StressScenarioRequest: { type: object, additionalProperties: false, required: [profileId, instrumentId, principal], properties: { profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }, instrumentId: { type: string, minLength: 1, maxLength: 50 }, principal: { type: number, minimum: 1000, maximum: 10000000 } } }
    StepUpProjectionRequest: { type: object, additionalProperties: false, required: [monthlyInvestment, annualReturnRate, years, annualStepUpRate], properties: { monthlyInvestment: { type: number, exclusiveMinimum: 0, maximum: 10000000 }, annualReturnRate: { type: number, exclusiveMinimum: -1, maximum: 1 }, years: { type: integer, minimum: 1, maximum: 50 }, annualStepUpRate: { type: number, minimum: 0, maximum: 1 } } }
    XirrRequest:
      oneOf:
        - type: object
          additionalProperties: false
          required: [cashflows]
          properties:
            cashflows:
              type: array
              minItems: 2
              maxItems: 1000
              items:
                type: object
                additionalProperties: false
                required: [amount, date]
                properties:
                  amount: { type: number }
                  date: { type: string, format: date }
            guess: { type: number, exclusiveMinimum: -1, maximum: 10 }
        - type: object
          additionalProperties: false
          required: [monthlySIP, months, currentValue]
          properties:
            monthlySIP: { type: number, exclusiveMinimum: 0, maximum: 100000000 }
            months: { type: integer, minimum: 1, maximum: 1200 }
            currentValue: { type: number, exclusiveMinimum: 0, maximum: 10000000000 }
    MonteCarloRequest: { type: object, additionalProperties: false, required: [profileId, instrument, monthly_investment, years], properties: { profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }, instrument: { type: string, maxLength: 50 }, monthly_investment: { type: number, exclusiveMinimum: 0, maximum: 10000000 }, years: { type: integer, minimum: 1, maximum: 30 }, target_amount: { type: number, minimum: 1000, maximum: 10000000000 } } }
    PortfolioMonteCarloRequest: { type: object, additionalProperties: false, required: [profileId, allocations, years], properties: { profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }, allocations: { $ref: '#/components/schemas/PortfolioWeights' }, years: { type: integer, minimum: 1, maximum: 30 }, target_amount: { type: number, minimum: 1000, maximum: 10000000000 } } }
    TaxCalculation: { type: object, required: [fiscalYear, policyVersion, inputsUsed], properties: { regime: { type: string, enum: [new, old] }, tax: { type: number }, taxAmount: { type: number }, effectiveRate: { type: number }, fiscalYear: { type: string, pattern: '^FY\d{4}-\d{2}$' }, policyVersion: { type: string }, inputsUsed: { type: object, additionalProperties: true }, rulesApplied: { type: array, items: { type: string } }, sourceReferences: { type: array, items: { type: object, additionalProperties: true } } }, additionalProperties: true }
    TaxRegimeComparison: { type: object, required: [tax, effective_rate, rebate_applied, taxable_income, standard_deduction, cess, nps80CCD2, allowed80D, slab_breakdown], properties: { tax: { type: number }, effective_rate: { type: number }, rebate_applied: { type: boolean }, taxable_income: { type: number }, standard_deduction: { type: number }, old_regime_deductions: { type: number }, marginal_relief_applied: { type: boolean }, marginal_relief_amount: { type: number }, cess: { type: number }, nps80CCD2: { type: number }, allowed80D: { type: number }, slab_breakdown: { type: array, items: { type: object, additionalProperties: true } } }, additionalProperties: true }
    TaxComparison: { type: object, required: [income, fiscal_year, verified, new_regime, old_regime, recommended_regime, saving, saving_pct, saving_with, calculation_classification, deduction_limits, remaining_deductions, optimized_old_regime_tax, potential_tax_saving, crossover_breakpoint], properties: { income: { type: number }, fiscal_year: { type: string, pattern: '^FY\d{4}-\d{2}$' }, verified: { type: boolean, const: true }, policyVersion: { type: string }, statuteMetadata: { type: object, additionalProperties: true }, verifiedRuleIds: { type: array, items: { type: string } }, sourceReferences: { type: array, items: { type: object, additionalProperties: true } }, rules: { type: object, additionalProperties: true }, inputsUsed: { type: object, additionalProperties: true }, rulesApplied: { type: array, items: { type: string } }, assumptions: { type: array, items: { type: string } }, unavailableReasons: { type: array, items: { type: string } }, new_regime: { $ref: '#/components/schemas/TaxRegimeComparison' }, old_regime: { $ref: '#/components/schemas/TaxRegimeComparison' }, recommended_regime: { type: string, enum: [old, new] }, saving: { type: number }, saving_pct: { type: number }, saving_with: { type: string, enum: [old, new] }, calculation_classification: { type: string, const: SEPARATE_TAX_WHAT_IF }, deduction_limits: { type: object, additionalProperties: true }, remaining_deductions: { type: object, additionalProperties: true }, optimized_old_regime_tax: { type: number }, potential_tax_saving: { type: number }, crossover_breakpoint: { type: [number, 'null'] } }, additionalProperties: true }
    TaxPolicyMetadata: { type: object, required: [currentFiscalYear, verifiedFiscalYears, policies], properties: { currentFiscalYear: { type: string }, currentFiscalYearVerified: { type: boolean }, verifiedFiscalYears: { type: array, items: { type: string } }, policies: { type: array, items: { type: object, required: [fiscalYear, policyVersion, sourceReferences], additionalProperties: true } } }, additionalProperties: false }
    TaxDeductionInputs: { type: object, additionalProperties: false, description: All deduction identifiers are compatibility input names, not current statutory authority. Positive unclassified other deductions are rejected; zero is accepted only for backward compatibility., properties: { section80C: { type: number, minimum: 0, maximum: 150000, description: Legacy input name; eligibility is fiscal-year and regime specific. }, nps80CCD1B: { type: number, minimum: 0, maximum: 50000, description: Legacy input name; eligibility is fiscal-year and regime specific. }, nps80CCD2: { type: number, minimum: 0 }, basicSalary: { type: number, minimum: 0 }, isGovtEmployee: { type: boolean }, section80D: { type: number, minimum: 0 }, section80D_self: { type: number, minimum: 0 }, section80D_parents: { type: number, minimum: 0 }, parents_senior: { type: boolean }, self_senior: { type: boolean }, hra: { type: number, minimum: 0 }, homeLoanInterest: { type: number, minimum: 0 }, section80EEA: { type: number, minimum: 0 }, savingsInterest: { type: number, minimum: 0 }, section80TTA: { type: number, minimum: 0 }, section80TTB: { type: number, minimum: 0 }, other: { type: number, minimum: 0, maximum: 0, description: Deprecated compatibility input; only zero is accepted. } } }
    TaxCalculationContextV2: { type: object, additionalProperties: false, required: [annualGrossIncome, incomeSource, regime, fiscalYear], properties: { fiscalYear: { type: string, pattern: '^FY\d{4}-\d{2}$' }, incomeSource: { type: string, enum: [salary, pension, family_pension, business, other] }, annualGrossIncome: { type: number, minimum: 0, maximum: 1000000000 }, regime: { type: string, enum: [new, old] }, userAge: { type: integer, minimum: 18, maximum: 120 }, deductions: { $ref: '#/components/schemas/TaxDeductionInputs' }, illustrativePrincipal: { type: number, exclusiveMinimum: 0, maximum: 1000000000 }, holdingPeriodMonths: { type: number, minimum: 0, maximum: 1200 }, acquisitionDate: { type: string, format: date }, redemptionDate: { type: string, format: date }, redemptionChannel: { type: string, enum: [RBI_REDEMPTION, MATURITY_REDEMPTION, SECONDARY_MARKET_SALE] }, acquiredAtOriginalIssue: { type: boolean }, heldContinuously: { type: boolean }, section112AExemptionUsed: { type: number, minimum: 0, maximum: 125000 } } }
    ProductPostTaxAnalysis: { type: object, required: [status, calculationClass, taxClass, fiscalYear, inputBasis, dataClass], properties: { status: { type: string, enum: [CALCULATED, REQUIRES_TAX_INPUTS, TAX_CLASSIFICATION_UNAVAILABLE, FISCAL_YEAR_UNSUPPORTED, PRODUCT_FACTS_UNAVAILABLE, UNAVAILABLE] }, calculationClass: { type: string, enum: [CURRENT_RATE_POST_TAX_ILLUSTRATION, HISTORICAL_RETURN_POST_TAX_ILLUSTRATION, MODELLED_POST_TAX_PROJECTION, TRANSACTION_TAX_ESTIMATE] }, taxClass: { type: [string, 'null'] }, taxClassification: { type: [string, 'null'] }, fiscalYear: { type: [string, 'null'] }, policyVersion: { type: [string, 'null'] }, statuteMetadata: { type: [object, 'null'], additionalProperties: true }, inputBasis: { type: string }, principal: { type: [number, 'null'] }, illustrativePrincipal: { type: [number, 'null'] }, grossGain: { type: [number, 'null'] }, taxableGain: { type: [number, 'null'] }, exemptionApplied: { type: [number, 'null'] }, incrementalTax: { type: [number, 'null'] }, cess: { type: [number, 'null'] }, surcharge: { type: [number, 'null'] }, netGain: { type: [number, 'null'] }, postTaxRatePct: { type: [number, 'null'] }, holdingPeriodBasis: { type: [string, 'null'] }, dataClass: { type: string }, requiredTaxInputs: { type: array, items: { type: string } }, unavailableReasons: { type: array, items: { type: string } }, assumptions: { type: array, items: { type: string } }, rulesApplied: { type: array, items: { type: string } }, metricLabel: { type: [string, 'null'] }, disclosure: { type: [string, 'null'] }, message: { type: [string, 'null'] }, isHistoricalEstimate: { type: boolean }, historicalObservationWindowMonths: { type: [integer, 'null'] }, sourceReferences: { type: array, items: { type: object, additionalProperties: true } } }, additionalProperties: false }
    PostTaxInstrument: { type: object, additionalProperties: false, required: [instrumentType, nominalRate, holdingYears, monthlySIP], properties: { instrumentType: { type: string, maxLength: 50 }, nominalRate: { type: number, minimum: 0, maximum: 1 }, holdingYears: { type: number, minimum: 0.01, maximum: 100 }, monthlySIP: { type: number, minimum: 0, maximum: 100000000 }, acquisitionDate: { type: string, format: date }, redemptionDate: { type: string, format: date }, redemptionChannel: { type: string, enum: [RBI_REDEMPTION, MATURITY_REDEMPTION, SECONDARY_MARKET_SALE] }, acquiredAtOriginalIssue: { type: boolean }, heldContinuously: { type: boolean }, couponRate: { type: number, minimum: 0, maximum: 1 }, annuityFraction: { type: number, minimum: 0, maximum: 1 }, retirementTiming: { type: string, minLength: 2, maxLength: 100 } } }
    PostTaxReturnRequestV2: { allOf: [{ $ref: '#/components/schemas/PostTaxInstrument' }, { type: object, additionalProperties: false, required: [annualIncome, regime, userAge, incomeSource, fiscalYear], properties: { annualIncome: { type: number, minimum: 0, maximum: 1000000000 }, regime: { type: string, enum: [new, old] }, userAge: { type: integer, minimum: 18, maximum: 120 }, incomeSource: { type: string, enum: [salary, pension, family_pension, business, other] }, fiscalYear: { type: string, pattern: '^FY\d{4}-\d{2}$' }, deductions: { $ref: '#/components/schemas/TaxDeductionInputs' }, section112AExemptionUsed: { type: number, minimum: 0, maximum: 125000 } } }], unevaluatedProperties: false }
    PostTaxReturnBatchRequestV2: { type: object, additionalProperties: false, required: [instruments, annualIncome, regime, userAge, incomeSource, inflationRate, fiscalYear], properties: { instruments: { type: array, minItems: 1, maxItems: 50, items: { $ref: '#/components/schemas/PostTaxInstrument' } }, annualIncome: { type: number, minimum: 0, maximum: 1000000000 }, regime: { type: string, enum: [new, old] }, userAge: { type: integer, minimum: 18, maximum: 120 }, incomeSource: { type: string, enum: [salary, pension, family_pension, business, other] }, inflationRate: { type: number, minimum: 0, maximum: 1 }, fiscalYear: { type: string, pattern: '^FY\d{4}-\d{2}$' }, deductions: { $ref: '#/components/schemas/TaxDeductionInputs' }, section112AExemptionUsed: { type: number, minimum: 0, maximum: 125000 } } }
    PostTaxReturnRequest: { allOf: [{ $ref: '#/components/schemas/PostTaxInstrument' }, { type: object, required: [annualIncome, regime, userAge, incomeSource, fiscalYear], properties: { annualIncome: { type: number, minimum: 0, maximum: 1000000000 }, regime: { type: string, enum: [new, old] }, userAge: { type: integer, minimum: 18, maximum: 120 }, incomeSource: { type: string, enum: [salary, pension, family_pension, business, other] }, fiscalYear: { type: string, pattern: '^FY\\d{4}-\\d{2}$' } } }], unevaluatedProperties: false }
    PostTaxReturnBatchRequest: { type: object, additionalProperties: false, required: [instruments, annualIncome, regime, userAge, incomeSource, inflationRate, fiscalYear], properties: { instruments: { type: array, minItems: 1, maxItems: 50, items: { $ref: '#/components/schemas/PostTaxInstrument' } }, annualIncome: { type: number, minimum: 0, maximum: 1000000000 }, regime: { type: string, enum: [new, old] }, userAge: { type: integer, minimum: 18, maximum: 120 }, incomeSource: { type: string, enum: [salary, pension, family_pension, business, other] }, inflationRate: { type: number, minimum: 0, maximum: 1 }, fiscalYear: { type: string, pattern: '^FY\\d{4}-\\d{2}$' } } }
    PortfolioOptimiseRequest: { type: object, additionalProperties: false, required: [profileId, assets, strategy], properties: { profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }, assets: { type: array, minItems: 2, maxItems: 20, uniqueItems: true, items: { type: string, maxLength: 50 } }, strategy: { type: string, enum: [min_variance, max_sharpe, risk_parity, max_return] } } }
    RebalanceRequest: { type: object, additionalProperties: false, required: [profileId, current_allocation, target_allocation, threshold, partial_ratio, holding_months], properties: { profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' }, current_allocation: { type: object, minProperties: 1, maxProperties: 30, additionalProperties: { type: number, minimum: 0, maximum: 1000000000 } }, target_allocation: { type: object, minProperties: 1, maxProperties: 30, additionalProperties: { type: number, minimum: 0, maximum: 100 }, description: Weights must sum to exactly 100. }, threshold: { type: number, minimum: 0, maximum: 50 }, partial_ratio: { type: number, minimum: 0.1, maximum: 1 }, holding_months: { type: number, minimum: 0, maximum: 600 } } }
    PortfolioResult: { type: object, properties: { weights: { type: object, additionalProperties: { type: number } }, trades: { type: array, items: { type: object, additionalProperties: true } } }, additionalProperties: true }
    ChatRequest: { type: object, additionalProperties: false, required: [message], properties: { message: { type: string, minLength: 1, maxLength: 1000 }, session_id: { type: string, maxLength: 100 } } }
    ChatResponse:
      type: object
      required: [version, response, session_id, latency_ms, grounded, provider, evidence_ids_used, unavailable_facts, validation_status, fallback, messages_this_hour, rate_limit_remaining, citations, action_cards]
      properties:
        version: { type: string }
        response: { type: string }
        session_id: { type: string }
        latency_ms: { type: number }
        grounded: { type: boolean }
        provider: { type: string }
        model: { type: [string, 'null'] }
        prompt_version: { type: [string, 'null'] }
        grounding_version: { type: [string, 'null'] }
        evidence_ids_used: { type: array, items: { type: string } }
        unavailable_facts: { type: array, items: { type: string } }
        validation_status: { type: [string, 'null'] }
        fallback: { type: boolean }
        messages_this_hour: { type: integer, minimum: 0 }
        rate_limit_remaining: { type: integer, minimum: 0 }
        citations:
          type: array
          items:
            type: object
            required: [citation_id, document_title, source, source_url, excerpt, evidence_class, observed_at, freshness]
            properties:
              citation_id: { type: string }
              document_title: { type: string }
              source: { type: string }
              source_url: { type: [string, 'null'], format: uri-reference }
              excerpt: { type: string }
              evidence_class: { type: [string, 'null'] }
              observed_at: { type: [string, 'null'], format: date-time }
              freshness: { type: [string, object, 'null'], additionalProperties: true }
            additionalProperties: false
        action_cards: { type: array, items: { type: object, additionalProperties: true } }
      additionalProperties: true
    RegimeAdjustRequest: { type: object, additionalProperties: false, required: [profileId], properties: { profileId: { type: string, pattern: '^[0-9a-fA-F]{24}$' } } }
    HealthStatus: { type: object, required: [status], properties: { status: { type: string }, timestamp: { type: string, format: date-time }, service: { type: string } }, additionalProperties: true }
